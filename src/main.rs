mod codec;
mod types;

use crate::codec::Settings;

fn main() {
    println!("Hello, world!");
}

fn flatten(_settings: &Settings) -> Settings {
    // let version = settings.version;
    // let new_properties
    // Settings {
    //     version,
    //     properties: settings
    // }
    todo!()
}
